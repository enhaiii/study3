// Реализовать объект calc, который содержит методы (только методы last и showHistory возвращают данные, остальные методы - нет, P.s. можно реализовать только для самопроверки):
// add() - сложить два числа
// sub() - вычесть из числа другое число
// mult() - перемножить числа
// div() - поделить числа c округлением до 2-ух знаков после запятой
// intDiv() - поделить числа нацело (opr: //)
// last() - вывод результата последней операции
// showHistory() - вывод всей истории результатов
// clear() - очистить историю
// undo() - отменить последнюю операцию

// Сохранять историю операций в объекте:
// Формат: [{a, opr, b, res}, ...]
// Пример: [
    {
    a: 1,
    opr: "+",
    b: 3,
    res: 4,
    },

    {
    a: 4,
    opr: "*",
    b: 5,
    res: 20,
},
...
]

// Формат вывода истории:

// История:
// 1 + 3 = 4
// 4 * 5 = 20
// ...

// Если история пустая, то вывести: История пуста

// Если в метод был передан только один аргумент, то за первый аргумент брать результат последней операции и продолжать историю операций, если в метод передано два оператора, то также необходимо очистить историю операций и результат последней операции
